purge_keep_days: 1
include:
  domains:
    - sensor
    - switch
exclude:
  entities:
    - sensor.last_boot
    - sensor.date
    - sensor.time_date
    - sensor.time
    - sensor.time_utc
  entity_globs:
    - sensor.weather_*
    - sensor.iphone*

